syntax = "proto3";


message Insults {
    repeated string insults = 10;
}

message Insult {
    string insult = 1;
}

message Empty {}

service Functions {
    rpc AddInsult(Insult) returns (Empty);
    rpc GetInsults(Empty) returns (Insults);
    rpc Insultme(Empty) returns (Insult);
}